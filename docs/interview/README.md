# Interview materials

The three numbered documents cover the architecture, resume wording, and engineering retrospectives.

Evidence reports contain provenance, commands, configuration references and board environment. Older runs did not record an execution commit: execution_commit=null is intentional; archived_commit only identifies the preserved source snapshot. Historical benchmark reproduction also requires the original input image, which is not published.

See ../pc-viewer.md, ../camera-investigation.md and ../deployment.md for the current demo and deployment workflow.
